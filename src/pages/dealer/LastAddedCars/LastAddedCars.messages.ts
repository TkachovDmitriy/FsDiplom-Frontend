import { defineMessages } from 'react-intl'

const scope = 'pages.dealer'

export default defineMessages({
  lastAddedTitle: {
    id: `${scope}.lastAddedTitle`,
    defaultMessage: "Last added dealer's cars"
  },
  seeAll: {
    id: `${scope}.seeAll`,
    defaultMessage: 'See all'
  }
})
