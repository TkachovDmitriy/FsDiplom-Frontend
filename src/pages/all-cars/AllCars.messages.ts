import { defineMessages } from 'react-intl'

const scope = 'pages.allCars'

export default defineMessages({
  totalCars: {
    id: `${scope}.totalCars`,
    defaultMessage: 'Cars'
  },
  totalCar: {
    id: `${scope}.totalCar`,
    defaultMessage: 'Car'
  }
})
