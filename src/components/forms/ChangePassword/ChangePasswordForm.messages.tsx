import { defineMessages } from 'react-intl'

const scope = 'components.forms.ChangePassword'

export default defineMessages({
  saveChanges: {
    id: `${scope}.button.saveChanges`,
    defaultMessage: 'Save changes'
  }
})
