import { defineMessages } from 'react-intl'

const scope = 'pages.dealer'

export default defineMessages({
  rulesForTestDrive: {
    id: `${scope}.rulesForTestDrive`,
    defaultMessage: 'Rules for test drive'
  },
  seeAll: {
    id: `${scope}.seeAll`,
    defaultMessage: 'See all'
  },
  showLess: {
    id: `${scope}.showLess`,
    defaultMessage: 'Show less'
  }
})
